---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-nvr
  namespace: home
spec:
  storageClassName: ""
  volumeName: nfs-nvr
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
