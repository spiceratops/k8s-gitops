---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: 5etools-config
  namespace: gaming
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ${CLUSTER_STORAGE_BLOCK}
