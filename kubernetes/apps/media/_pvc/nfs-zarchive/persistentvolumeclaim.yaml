---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-zarchive
  namespace: media
spec:
  storageClassName: ""
  volumeName: nfs-zarchive
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
