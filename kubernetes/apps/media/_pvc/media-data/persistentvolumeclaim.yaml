---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-data
  namespace: media
spec:
  storageClassName: media-data
  volumeName: media-data
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
