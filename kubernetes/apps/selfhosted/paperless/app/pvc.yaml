---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 50Gi
  storageClassName: ${CLUSTER_STORAGE_FILESYSTEM}
