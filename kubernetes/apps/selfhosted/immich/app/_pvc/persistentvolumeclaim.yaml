---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich
  namespace: selfhosted
spec:
  storageClassName: immich
  volumeName: immich
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
