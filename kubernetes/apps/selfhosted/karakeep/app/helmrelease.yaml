---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app karakeep
  namespace: selfhosted
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  dependsOn: []
  interval: 1h

  values:
    controllers:
      karakeep:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/karakeep-app/karakeep
              tag: 0.33.2@sha256:b069e4307dec06ea06d16989c6861c30a1ff208568be44ed5fb5d422cd3e950c
            env:
              DATA_DIR: /data
              MEILI_ADDR: http://karakeep-meilisearch:7700
              BROWSER_WEB_URL: http://karakeep-browserless:9222
              NEXTAUTH_URL: https://keep.${SECRET_DOMAIN}
              CRAWLER_DOWNLOAD_BANNER_IMAGE: true
              CRAWLER_ENABLE_ADBLOCKER: true
              OPENAI_BASE_URL: http://litellm.ai.svc.cluster.local
              # OAuth setup
              OAUTH_PROVIDER_NAME: Pocket-ID
              OAUTH_WELLKNOWN_URL: https://id.${SECRET_DOMAIN}/.well-known/openid-configuration
              DISABLE_PASSWORD_AUTH: "true"
              OAUTH_ALLOW_DANGEROUS_EMAIL_ACCOUNT_LINKING: "true"
            envFrom: &envFrom
              - secretRef:
                  name: karakeep-secret
            resources:
              requests:
                cpu: 10m
                memory: 256Mi
              limits:
                memory: 750Mi
          meilisearch:
            image:
              repository: getmeili/meilisearch
              tag: v1.34.3@sha256:82767d2afc4fb6c3e4ebadb134ca6ef5687c894ae9e152928619393b1f331681
            env:
              MEILI_NO_ANALYTICS: "true"
            envFrom: *envFrom
            args:
              - /bin/meilisearch
              - --experimental-dumpless-upgrade
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 2Gi
          browserless:
            image:
              repository: ghcr.io/browserless/chromium
              tag: v2.56.4@sha256:5252f0d0bb947acec41c39f4507fab4af15f9f9337267ca1a97fa026197df8e6
            env:
              CONCURRENT: 10
              DATA_DIR: /profile
              DOWNLOAD_DIR: /downloads
              ENABLE_DEBUGGER: false
              EXTERNAL: http://karakeep-browserless.selfhosted.svc.cluster.local
              HEALTH: true
              SCREEN_DEPTH: 16
              SCREEN_HEIGHT: 1024
              SCREEN_WIDTH: 1920
              TIMEOUT: 600000 # 10 minutes
              TOKEN:
                valueFrom:
                  secretKeyRef:
                    name: karakeep-secret
                    key: BROWSERLESS_TOKEN
              TZ: ${TIMEZONE}
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: 9222
              readiness: *probe
            securityContext:
              allowPrivilegeEscalation: false
    service:
      app:
        controller: karakeep
        ports:
          http:
            port: &port 3000
      browserless:
        controller: karakeep
        ports:
          http:
            port: 9222
      meilisearch:
        controller: karakeep
        ports:
          http:
            port: 7700
    route:
      app:
        hostnames:
          - keep.${SECRET_DOMAIN}
          - "{{ .Release.Name }}.${SECRET_DOMAIN}"
          - hoarder.${SECRET_DOMAIN}
        parentRefs:
          - name: envoy-internal
            namespace: networking
            sectionName: https
        rules:
          - backendRefs:
              - name: karakeep-app
                port: *port
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/icon: karakeep.png
          gethomepage.dev/name: Karakeep
          gethomepage.dev/url: keep.${SECRET_DOMAIN}
          gethomepage.dev/group: The Internets
          gethomepage.dev/description: Saved Web Content
    persistence:
      config:
        existingClaim: "{{ .Release.Name }}"
        advancedMounts:
          karakeep:
            app:
              - path: /data
                subPath: data
            meilisearch:
              - path: /meili_data
                subPath: meili_data
      browserless:
        type: emptyDir
        sizeLimit: 1Gi
        advancedMounts:
          browserless:
            browserless:
              - path: /profile
                subPath: profile
              - path: /downloads
                subPath: downloads
      run:
        type: emptyDir
        medium: Memory
        sizeLimit: 100Mi
      tmp:
        type: emptyDir
        medium: Memory
        sizeLimit: 1Gi
