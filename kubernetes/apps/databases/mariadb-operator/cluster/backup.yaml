---
apiVersion: k8s.mariadb.com/v1alpha1
kind: Backup
metadata:
  name: backup
  namespace: databases
spec:
  mariaDbRef:
    name: mariadb
  maxRetention: 720h # 30 days
  storage:
    s3:
      bucket: mariadb
      endpoint: https://s3.${PRIVATE_DOMAIN}:9000
      region: home
      accessKeyIdSecretKeyRef:
        name: mariadb-secret
        key: access-key-id
      secretAccessKeySecretKeyRef:
        name: mariadb-secret
        key: secret-access-key
      tls:
        enabled: true
