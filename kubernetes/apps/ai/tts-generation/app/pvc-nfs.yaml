---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: tts-generation-models
spec:
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: mnas.${PUBLIC_DOMAIN}
    path: /ai/models/ttsgen
  mountOptions:
    - nfsvers=4.2
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tts-generation-models
  namespace: ai
spec:
  storageClassName: ""
  volumeName: tts-generation-models
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
