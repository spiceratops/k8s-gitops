---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models
  namespace: ai
spec:
  storageClassName: ${CLUSTER_STORAGE_HOSTPATH}
  volumeName: ollama-models
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
